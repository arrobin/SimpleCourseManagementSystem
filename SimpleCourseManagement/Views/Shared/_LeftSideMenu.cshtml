<div class="col-md-2">
    <div class="sidebar content-box" style="display: block;">
        <ul class="nav">
            <!-- Main menu -->
            <li class="current"><a href="@Url.Action("Index","Home")"><i class="glyphicon glyphicon-dashboard"></i> Dashboard</a></li>
            @{
                if ((int)Session["UserRoleId"] == 1)
                {
                    <li class="submenu">
                        <a href="#">
                            <i class="glyphicon glyphicon-list-alt"></i> Course
                            <span class="caret pull-right"></span>
                        </a>
                        <!-- Sub menu -->
                        <ul>
                            <li>@Html.ActionLink("Course Add", "Create", "Courses")</li>
                            <li>@Html.ActionLink("Course List", "Index", "Courses")</li>
                        </ul>
                    </li>
                    <li class="submenu">
                        <a href="#">
                            <i class="glyphicon glyphicon-th"></i> Batch
                            <span class="caret pull-right"></span>
                        </a>
                        <!-- Sub menu -->
                        <ul>
                            <li>@Html.ActionLink("Batch Add", "Create", "Batches")</li>
                            <li>@Html.ActionLink("Batch List", "Index", "Batches")</li>
                        </ul>
                    </li>
                    <li><a href="@Url.Action("ResultAdd","Trainees")"><i class="glyphicon glyphicon-upload"></i> Result Add</a></li>
                    <li><a href="@Url.Action("Index","Trainees")"><i class="glyphicon glyphicon-list"></i> Trainee List</a></li>
                }
                else
                {
                    <li><a href="@Url.Action("Create","Trainees")"><i class="glyphicon glyphicon-list"></i> Registration</a></li>
                }
            }



        </ul>
    </div>
</div>
